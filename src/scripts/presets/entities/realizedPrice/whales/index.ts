import { applyPreset } from './apply'

import description from './description.md?raw'

export const whalesRealizedPricePreset = {
  id: 'whalesRealizedPricePreset',
  title: 'Whales Realized Price',
  applyPreset,
  description,
}
