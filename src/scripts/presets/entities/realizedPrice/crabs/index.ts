import { applyPreset } from './apply'

import description from './description.md?raw'

export const crabsRealizedPricePreset = {
  id: 'crabsRealizedPricePreset',
  title: 'Crabs Realized Price',
  apply: applyPreset,
  description,
}
