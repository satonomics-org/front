import { applyPreset } from './apply'

import description from './description.md?raw'

export const sixMonthsPreset = {
  id: '<6M',
  title: '<6M Holders Realized Price',
  apply: applyPreset,
  description,
}
