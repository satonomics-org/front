import { applyPreset } from './apply'

import description from './description.md?raw'

export const oneYearPreset = {
  id: '<1Y',
  title: '<21 Holders Realized Price',
  apply: applyPreset,
  description,
}
