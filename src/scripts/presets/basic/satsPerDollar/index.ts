import { applyPreset } from './apply'
import description from './description.md?raw'

export const satsPerDollarPreset = {
  id: 'satsPerDollar',
  title: 'Sats per Dollar',
  applyPreset,
  description,
}
