import { applyPreset } from './apply'

import description from './description.md?raw'

export const localExtremesPreset = {
  id: 'localExtremes',
  title: 'Local Extremes',
  applyPreset,
  description,
}
