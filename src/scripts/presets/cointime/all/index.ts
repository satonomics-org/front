import { applyPreset } from './apply'

import description from './description.md?raw'

export const allCointimePricesPreset = {
  id: 'allCoinTime',
  title: 'All Cointime Prices',
  applyPreset,
  description,
}
