import { applyPreset } from './apply'
import description from './description.md?raw'

export const allAveragesPreset = {
  id: 'allMAs',
  title: 'All Moving Averages',
  applyPreset,
  description,
}
