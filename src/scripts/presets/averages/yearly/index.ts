import { applyPreset } from './apply'
import description from './description.md?raw'

export const yearlyMAPreset = {
  id: 'yearlyMA',
  title: 'Yearly Moving Average',
  applyPreset,
  description,
}
