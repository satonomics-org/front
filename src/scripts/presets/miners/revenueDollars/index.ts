import { applyPreset } from './apply'
import description from './description.md?raw'

export const minersRevenueDollarsPreset = {
  id: 'minersDollarsRevenue',
  title: 'Revenue in Dollars',
  applyPreset,
  description,
}
