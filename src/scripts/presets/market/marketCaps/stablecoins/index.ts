import { applyPreset } from './apply'
import description from './description.md?raw'

export const stablecoinsMarketCapsPreset = {
  id: 'stablecoinsMarketCaps',
  title: 'Stablecoins Market Caps',
  applyPreset,
  description,
}
